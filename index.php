<?php
// เรียกใช้ไฟล์ db.php เพื่อเชื่อมต่อฐานข้อมูล
// ไฟล์นี้ต้องมีตัวแปร $conn ที่ใช้เชื่อม MySQL
require_once "db.php";

/*
    สั่ง SQL Query ไปที่ฐานข้อมูล
    SELECT * FROM products  = ดึงข้อมูลทุกคอลัมน์จากตาราง products
    ORDER BY id DESC        = เรียงข้อมูลจาก id มาก → น้อย (ล่าสุดขึ้นก่อน)
*/
$result = $conn->query("SELECT * FROM products ORDER BY id DESC");
?>

<!DOCTYPE html>
<html>
<head>
    <!-- ชื่อหน้าเว็บที่แสดงบนแท็บ -->
    <title>Mythic Craft Admin</title>

    <!-- เรียกใช้ Bootstrap CSS จาก CDN เพื่อจัดรูปแบบให้สวย -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<!-- p-4 = padding รอบตัวหน้าเว็บ -->
<body class="p-4">

<div class="container">
    <!-- หัวข้อหน้า -->
    <h2>Product List</h2>

    <!-- ปุ่มเพิ่มสินค้า -->
    <!-- กดแล้วไปหน้า add.php -->
    <a href="add.php" class="btn btn-success mb-3">
        ➕ Add Product
    </a>

    <!-- ตารางแสดงข้อมูล -->
    <!-- table-bordered = มีเส้นขอบ -->
    <table class="table table-bordered">

        <!-- ส่วนหัวของตาราง -->
        <!-- table-dark = พื้นหลังสีดำ -->
        <thead class="table-dark">
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Price</th>
                <th>Status</th>
                <th>Action</th>
            </tr>
        </thead>

        <!-- ส่วนข้อมูลในตาราง -->
        <tbody>

        <?php
        /*
            while loop ทำงานตราบใดที่ยังมีข้อมูลใน $result
            fetch_assoc() = ดึงข้อมูลทีละแถวในรูปแบบ array
        */
        while($row = $result->fetch_assoc()):
        ?>
            <tr>

                <!-- แสดงค่า id -->
                <td><?= $row['id']; ?></td>

                <!-- แสดงชื่อสินค้า -->
                <td><?= $row['name']; ?></td>

                <!-- แสดงราคา -->
                <td><?= $row['price']; ?></td>

                <!-- แสดงสถานะสินค้า -->
                <td><?= $row['stock_status']; ?></td>

                <!-- ปุ่มจัดการ -->
                <td>

                    <!-- ปุ่มแก้ไข -->
                    <!-- ส่ง id ไปหน้า edit.php ผ่าน URL -->
                    <a href="edit.php?id=<?= $row['id']; ?>" 
                       class="btn btn-warning btn-sm">
                       ✏️ Edit
                    </a>

                    <!-- ปุ่มลบ -->
                    <!-- ส่ง id ไปหน้า delete.php -->
                    <!-- confirm() คือ popup ถามก่อนลบ -->
                    <a href="delete.php?id=<?= $row['id']; ?>" 
                       onclick="return confirm('Delete this product?')" 
                       class="btn btn-danger btn-sm">
                       🗑️ Delete
                    </a>

                </td>
            </tr>

        <!-- จบ loop -->
        <?php endwhile; ?>

        </tbody>
    </table>
</div>

</body>
</html>